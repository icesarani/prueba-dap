version: '3.8'

services:
  evaluator:
    build: .
    image: easy-evaluator:latest
    stdin_open: true
    tty: false
    volumes:
      - ./evaluation-scripts:/app/evaluation-scripts:ro
      - ./users:/app/users:ro
    network_mode: none  # No network access needed for evaluation
    profiles:
      - test